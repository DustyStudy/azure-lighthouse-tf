name: Deploy (reusable)

on:
  workflow_call:
    inputs:
      cloud:
        description: "public or gov"
        required: true
        type: string
      environment_name:
        description: "GitHub Environment to run under (gates OIDC credential + required reviewers)"
        required: true
        type: string

permissions:
  contents: read

jobs:
  apply:
    name: terraform apply (${{ inputs.cloud }})
    runs-on: ubuntu-latest
    # The GitHub Environment is what actually enforces "require reviewers"
    # and scopes which OIDC federated credential can be exchanged - the
    # federated credential subject is bound to this exact environment name,
    # per cloud, so a run against `gov-prod` can never mint a token that
    # only the `public-prod` App Registration trusts, or vice versa.
    environment: ${{ inputs.environment_name }}
    # Template mode: skip cleanly if this clone hasn't been wired up to a
    # real Azure subscription yet (no AZURE_CLIENT_ID_<cloud> repo variable
    # set). See README.md "One-time setup" for what an adopting team does
    # before this job can actually run.
    if: ${{ vars[format('AZURE_CLIENT_ID_{0}', inputs.cloud)] != '' }}
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4.2.2 - verified SHA

      # Guard every subsequent step individually (step-level `if:`, not
      # job-level) - the `matrix` context is documented as unavailable at
      # jobs.<job_id>.if, and rather than assume `inputs` behaves
      # differently there, this uses the one pattern confirmed to work
      # everywhere: a step sets an output, later steps check it.
      - name: Check Azure config for ${{ inputs.cloud }}
        id: config
        run: |
          if [ -n "${{ vars[format('AZURE_CLIENT_ID_{0}', inputs.cloud)] }}" ]; then
            echo "configured=true" >> "$GITHUB_OUTPUT"
          else
            echo "configured=false" >> "$GITHUB_OUTPUT"
            echo "::notice::No AZURE_CLIENT_ID_${{ inputs.cloud }} repo variable set - skipping deploy for ${{ inputs.cloud }}. Expected on an unconfigured template clone; see README.md 'Adopting this template'."
          fi

      - uses: hashicorp/setup-terraform@v4.0.1 # TODO: Dependabot will PR the verified SHA for this
        if: steps.config.outputs.configured == 'true'
        with:
          terraform_version: "1.9.0"

      - name: Azure login (OIDC)
        if: steps.config.outputs.configured == 'true'
        uses: azure/login@v3.0.2 # TODO: Dependabot will PR the verified SHA for this
        with:
          client-id: ${{ vars[format('AZURE_CLIENT_ID_{0}', inputs.cloud)] }}
          tenant-id: ${{ vars[format('AZURE_TENANT_ID_{0}', inputs.cloud)] }}
          subscription-id: ${{ vars[format('AZURE_SUBSCRIPTION_ID_{0}', inputs.cloud)] }}
          environment: ${{ inputs.cloud == 'gov' && 'AzureUSGovernment' || 'AzureCloud' }}

      - name: terraform init
        if: steps.config.outputs.configured == 'true'
        env:
          ARM_USE_OIDC: "true"
          ARM_USE_AZUREAD: "true"
        run: terraform init -backend-config=backend-config/${{ inputs.cloud }}.backend.hcl

      - name: Re-run prechecks against current state (defense in depth)
        if: steps.config.outputs.configured == 'true'
        run: |
          chmod +x scripts/prechecks/*.sh
          ./scripts/prechecks/validate-role-defs.sh environments/${{ inputs.cloud }}/terraform.tfvars

      - name: terraform plan
        if: steps.config.outputs.configured == 'true'
        env:
          ARM_USE_OIDC: "true"
          ARM_USE_AZUREAD: "true"
        run: |
          terraform plan \
            -var-file=environments/${{ inputs.cloud }}/terraform.tfvars \
            -out=tfplan
          terraform show -json tfplan > plan.json

      - name: Check for destructive changes to existing delegations
        if: ${{ steps.config.outputs.configured == 'true' && !contains(github.event.pull_request.labels.*.name, 'lighthouse-destructive-override') }}
        run: |
          chmod +x scripts/prechecks/check-existing-delegation.sh
          ./scripts/prechecks/check-existing-delegation.sh plan.json

      - name: terraform apply
        if: steps.config.outputs.configured == 'true'
        env:
          ARM_USE_OIDC: "true"
          ARM_USE_AZUREAD: "true"
        run: terraform apply -auto-approve tfplan
